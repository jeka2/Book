<%= form_for @group do |f| %>
	Group Name: <%= f.text_field :name %>
				<%= f.hidden_field :subscribed, :value => true %>
				<%= f.hidden_field :book_id, :value => params[:book_id].to_i %>
				<%= f.submit %>
<% end %>