<%= image_tag @user.avatar.url(:thumb) if @user.avatar? %>
<ul id="books">
  <% @books.each do |book| %>
    <li id="<%= book.name %>_info">
      <%= book.name %>
    </li>
  <% end %>
</ul>
